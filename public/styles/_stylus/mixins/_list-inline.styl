// Depends on list-unstyled
list-inline(space = false, no-margin = true)
  list-unstyled(no-margin)
  & > li
    display inline-block
  if space
    & > li + li
      margin-left space
$list-inline
  list-inline()